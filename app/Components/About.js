import React from 'react'
import styles from '../styles/index'

export default function About () {
	return (
		<p style={styles.about}> This app was built with react, es6, webpack, babel, react-router, and uses the wikipedia, and mapbox api. </p>
	)
}
